name: Deploy to AWS EC2 with ECR

on:
  push:
    branches: [main]
  workflow_dispatch:

env:
  ACTIONS_ALLOW_USE_UNSECURE_NODE_VERSION: true

jobs:
  deploy:
    name: Build & Deploy
    runs-on: ubuntu-latest
    
    steps:
    # ========================================
    # SETUP
    # ========================================
    - name: Checkout code
      uses: actions/checkout@v4
      
    - name: Create .env file from secret
      run: |
        echo '${{ secrets.ENV_FILE }}' > .env
        
    - name: Load environment variables
      run: |
        # Extract specific variables we need (safer approach)
        echo "AWS_REGION=$(grep '^AWS_REGION=' .env | cut -d'=' -f2- | tr -d '\"')" >> $GITHUB_ENV
        echo "ECR_REGISTRY=$(grep '^ECR_REGISTRY=' .env | cut -d'=' -f2- | tr -d '\"')" >> $GITHUB_ENV
        echo "AWS_ACCESS_KEY_ID=$(grep '^AWS_ACCESS_KEY_ID=' .env | cut -d'=' -f2- | tr -d '\"')" >> $GITHUB_ENV
        echo "AWS_SECRET_ACCESS_KEY=$(grep '^AWS_SECRET_ACCESS_KEY=' .env | cut -d'=' -f2- | tr -d '\"')" >> $GITHUB_ENV
        echo "EC2_HOST=$(grep '^EC2_HOST=' .env | cut -d'=' -f2- | tr -d '\"')" >> $GITHUB_ENV
        
        # Handle SSH key separately (multiline)
        EC2_SSH_KEY=$(awk '/^EC2_SSH_KEY=/{flag=1; sub(/^EC2_SSH_KEY=/, ""); print; next} flag && /^[A-Z_]+=/{flag=0} flag{print}' .env | tr -d '"')
        echo "EC2_SSH_KEY<<EOF" >> $GITHUB_ENV
        echo "$EC2_SSH_KEY" >> $GITHUB_ENV
        echo "EOF" >> $GITHUB_ENV
      
    - name: Configure AWS credentials
      uses: aws-actions/configure-aws-credentials@v4
      with:
        aws-access-key-id: ${{ env.AWS_ACCESS_KEY_ID }}
        aws-secret-access-key: ${{ env.AWS_SECRET_ACCESS_KEY }}
        aws-region: ${{ env.AWS_REGION }}
        
    - name: Login to Amazon ECR
      uses: aws-actions/amazon-ecr-login@v2
      
    - name: Set up Docker Buildx
      uses: docker/setup-buildx-action@v3
      
    - name: Get commit SHA
      id: vars
      run: echo "sha_short=$(git rev-parse --short HEAD)" >> $GITHUB_OUTPUT
      
    # ========================================
    # BUILD & PUSH IMAGES
    # ========================================
    - name: Build and push Gateway
      uses: docker/build-push-action@v5
      with:
        context: ./Boversal.Gateway
        push: true
        tags: |
          ${{ env.ECR_REGISTRY }}/boversal-gateway:latest
          ${{ env.ECR_REGISTRY }}/boversal-gateway:${{ steps.vars.outputs.sha_short }}
        cache-from: type=gha
        cache-to: type=gha,mode=max
        
    - name: Build and push ProjectManagement
      uses: docker/build-push-action@v5
      with:
        context: ./ProjectManagementService.API
        push: true
        tags: |
          ${{ env.ECR_REGISTRY }}/boversal-project-management:latest
          ${{ env.ECR_REGISTRY }}/boversal-project-management:${{ steps.vars.outputs.sha_short }}
        cache-from: type=gha
        cache-to: type=gha,mode=max
        
    - name: Build and push Utility
      uses: docker/build-push-action@v5
      with:
        context: ./UtilityService
        push: true
        tags: |
          ${{ env.ECR_REGISTRY }}/boversal-utility:latest
          ${{ env.ECR_REGISTRY }}/boversal-utility:${{ steps.vars.outputs.sha_short }}
        cache-from: type=gha
        cache-to: type=gha,mode=max
        
    # ========================================
    # PREPARE DEPLOYMENT
    # ========================================
    - name: Create deployment files
      run: |
        # Use the same .env file for deployment (already created from ENV_FILE secret)
        
        # Docker Compose file for ECR
        cat > docker-compose.ecr.yml << 'EOF'
        services:
          gateway:
            image: ${ECR_REGISTRY}/boversal-gateway:${IMAGE_TAG}
            ports:
              - "80:8080"
            environment:
              - ASPNETCORE_ENVIRONMENT=Production
              - ASPNETCORE_URLS=http://+:8080
              - DATABASE_URL=${DATABASE_URL}
              - JWT_KEY=${JWT_KEY}
              - JWT_ISSUER=${JWT_ISSUER}
              - JWT_AUDIENCE=${JWT_AUDIENCE}
              - JWT_EXPIRATION_HOURS=${JWT_EXPIRATION_HOURS}
            restart: unless-stopped
            depends_on:
              - project-management
              - utility

          project-management:
            image: ${ECR_REGISTRY}/boversal-project-management:${IMAGE_TAG}
            ports:
              - "8081:8081"
            environment:
              - ASPNETCORE_ENVIRONMENT=Production
              - ASPNETCORE_URLS=http://+:8081
              - DATABASE_URL=${DATABASE_URL}
              - JWT_KEY=${JWT_KEY}
              - KAFKA_BOOTSTRAP_SERVERS=${KAFKA_BOOTSTRAP_SERVERS}
            restart: unless-stopped

          utility:
            image: ${ECR_REGISTRY}/boversal-utility:${IMAGE_TAG}
            ports:
              - "8082:8082"
            environment:
              - ASPNETCORE_ENVIRONMENT=Production
              - ASPNETCORE_URLS=http://+:8082
              - EMAIL_SMTP_HOST=${EMAIL_SMTP_HOST}
              - EMAIL_SMTP_PORT=${EMAIL_SMTP_PORT}
              - EMAIL_SMTP_USER=${EMAIL_SMTP_USER}
              - EMAIL_SMTP_PASSWORD=${EMAIL_SMTP_PASSWORD}
              - EMAIL_FROM_ADDRESS=${EMAIL_FROM_ADDRESS}
              - EMAIL_FROM_NAME=${EMAIL_FROM_NAME}
              - AWS_REGION=${AWS_REGION}
              - AWS_BUCKET_NAME=${AWS_BUCKET_NAME}
            restart: unless-stopped
        EOF
        
        # Add IMAGE_TAG to .env for deployment
        echo "IMAGE_TAG=${{ steps.vars.outputs.sha_short }}" >> .env
        
    - name: Copy files to EC2
      uses: appleboy/scp-action@v0.1.7
      with:
        host: ${{ env.EC2_HOST }}
        username: ec2-user
        key: ${{ env.EC2_SSH_KEY }}
        source: ".env,docker-compose.ecr.yml"
        target: "/home/ec2-user/app-${{ steps.vars.outputs.sha_short }}/"
        
    # ========================================
    # DEPLOY ON EC2
    # ========================================
    - name: Deploy on EC2
      uses: appleboy/ssh-action@v1.0.3
      with:
        host: ${{ env.EC2_HOST }}
        username: ec2-user
        key: ${{ env.EC2_SSH_KEY }}
        command_timeout: 10m
        script: |
          set -e
          
          DEPLOY_DIR="/home/ec2-user/app-${{ steps.vars.outputs.sha_short }}"
          cd "$DEPLOY_DIR"
          
          # Login to ECR
          aws ecr get-login-password --region ${{ env.AWS_REGION }} | \
            docker login --username AWS --password-stdin ${{ env.ECR_REGISTRY }}
          
          # Load environment
          export $(cat .env | xargs)
          
          echo "📦 Pulling images..."
          docker compose -f docker-compose.ecr.yml pull
          
          # Zero-downtime deployment
          if [ -L /home/ec2-user/current ]; then
            OLD_DIR=$(readlink /home/ec2-user/current)
          fi
          
          echo "🚀 Starting containers..."
          docker compose -f docker-compose.ecr.yml up -d
          
          # Health check
          echo "🔍 Health checking..."
          for i in {1..20}; do
            if curl -sf http://localhost:80/health > /dev/null 2>&1; then
              echo "✅ Gateway healthy ($i/20)"
              break
            fi
            if [ $i -eq 20 ]; then
              echo "❌ Health check failed"
              docker compose -f docker-compose.ecr.yml logs --tail=20
              exit 1
            fi
            echo "⏳ Waiting for services... ($i/20)"
            sleep 5
          done
          
          # Atomic switch
          ln -sfn "$DEPLOY_DIR" /home/ec2-user/current
          
          # Stop old deployment
          if [ -n "$OLD_DIR" ] && [ "$OLD_DIR" != "$DEPLOY_DIR" ]; then
            cd "$OLD_DIR" && docker compose -f docker-compose.ecr.yml down || true
          fi
          
          # Cleanup
          cd /home/ec2-user
          ls -dt app-* | tail -n +4 | xargs rm -rf 2>/dev/null || true
          docker image prune -f
          
          echo "✅ Deployment completed!"
          echo "🌐 Access: http://$(curl -s http://169.254.169.254/latest/meta-data/public-ipv4)"
          echo "📝 Version: ${{ steps.vars.outputs.sha_short }}"
